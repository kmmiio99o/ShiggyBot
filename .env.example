# Example .env for ShiggyBot
# Copy this file to `.env` and fill in the values. DO NOT commit your real tokens to source control.
#########################
# Required (must set)
#########################
# Your bot token (starts with "Bot " in some contexts when used in code, but store token alone here)
DISCORD_TOKEN=your_bot_token_here
# Your Discord Application / Client ID (used for command registration, optional but recommended)
CLIENT_ID=your_application_client_id
# Optional development guild ID — when set, commands register to this guild instantly (fast dev loop)
DEV_GUILD_ID=optional_dev_guild_id_for_fast_registration
# Prefix for legacy prefix commands (single character or string). Example: S
# If not set, defaults to "S"
PREFIX=S
#########################
# Autorole configuration
#########################
# Role ID to assign automatically when users join. If not set, the scaffold uses a built-in fallback role.
WELCOME_ROLE_ID=1427396865711673484
# Legacy / optional default role used by some commands (not required if you don't use /getrole)
DEFAULT_ROLE_ID=1427396865711673484
#########################
# Presence (activities) configuration
#########################
# Status: one of online, idle, dnd, invisible
PRESENCE_STATUS=idle
# Webhook logging for error's/warnings and other things
WEBHOOK_URL=LOG_WEBHOOK_URL=https://discord.com/api/webhooks/<other_part>
# Rotation interval in seconds (default in this scaffold: 5). Production: use 30+ seconds to be safe.
PRESENCE_INTERVAL=5
# Activities override. Supports:
#  - JSON array of objects: [{"name":"for /getrole","type":"WATCHING"}, ...]
#  - Or TYPE:Text separated by | , e.g. "WATCHING:for /getrole|LISTENING:role requests|PLAYING:the server"
#  - Or simple CSV of names (defaults to PLAYING)
PRESENCE_ACTIVITIES=WATCHING:welcoming new members|PLAYING:assigning roles|LISTENING:use /help
#########################
# Environment
#########################
# NODE_ENV=development or production
NODE_ENV=development
#########################
# Notes / Security
#########################
# - Keep DISCORD_TOKEN and CLIENT_ID secret. Use your host's environment/secret manager for production.
# - Role IDs and Guild IDs are numeric strings (copy them from Discord's Developer Tools / right-click -> Copy ID).
# - If you need per-guild configuration (different welcome role per guild), consider adding a persistence layer
#   and a slash command (e.g., /setautorole) rather than editing this file.
# - If the bot is in 100+ guilds and you use the Guild Members intent, you must verify your bot with Discord.
